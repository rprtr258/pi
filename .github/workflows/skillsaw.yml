name: skillsaw

on:
  push:
    branches: [master]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Lint skills
        uses: stbenjam/skillsaw@v0.20.0
        with:
          path: agent/skills
